# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing how to write asynchronous code

GET /byStr/:str/:limit                     controllers.SuppliersController.getItemLike(str:String,limit:Int)
GET /byName/:name                     controllers.SuppliersController.getItem(name:String)
